using NUnit.Framework;

namespace Food.Services.Tests.Controllers
{
    [TestFixture]
    public class NotificationControllerTests
    {
        /*
        private FakeContext _context;
        private NotificationController _controller;
        private Mock<Accessor> _accessor;
        private readonly Random _random = new Random();
        private User _user;

        private void SetUp()
        {
            _accessor = new Mock<Accessor>();
            _context = new FakeContext();
            _controller = new NotificationController(_context, _accessor.Object);
            ContextManager.Set(_context);
            _user = UserFactory.CreateUser();
            var identity = new ClaimsIdentity();
            identity.AddClaim(new Claim(ClaimTypes.NameIdentifier, _user.Id.ToString()));
            Thread.CurrentPrincipal = new GenericPrincipal(identity, null);
        }

        [TestMethod()]
        public void GetNotificationForUserTest()
        {
            Assert.Fail();
        }

        [TestMethod()]
        public void StopNotifyUserTest()
        {
            Assert.Fail();
        }

        [TestMethod()]
        public void SetOrdersViewedTest()
        {
            Assert.Fail();
        }

        [TestMethod()]
        public void NewOrderForNotificationTest()
        {
            Assert.Fail();
        }*/
    }
}